import 'package:flutter/material.dart';
import 'components/wide_list_body.dart';

class WideListScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
